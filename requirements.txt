# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic>=2.7.4,<3.0.0
python-dotenv==1.0.1
httpx==0.27.0

# LangChain
langchain==0.3.0
langchain-groq==0.2.0
langchain-huggingface==0.1.0
langchain-community==0.3.0
langchain-chroma>=0.2.0,<0.3.0

# Local embeddings backend
sentence-transformers==3.0.1

# Vector store — chromadb 1.x ships prebuilt Rust binaries on Windows,
# so chroma-hnswlib (C++) is no longer required.
chromadb>=1.0.0,<2.0.0

# Document loading
pypdf==4.3.1
# unstructured removed — not used by our loaders (TextLoader + PyPDFLoader)
# and has no Python 3.13 wheel at the pinned version

# APIs
resend==2.3.0

# Utils
python-multipart==0.0.9
aiofiles==23.2.1
